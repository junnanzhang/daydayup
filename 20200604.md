记录一个经典的面试问题

```
function test(person) {
  person.age = 26
  person = {
    name: 'aaa',
    age: 30
  }

  return person
}
const p1 = {
  name: 'bbb',
  age: 25
}
const p2 = test(p1)
console.log(p1) // -> ?
console.log(p2) // -> ?
```